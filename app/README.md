# TutorBank.xcodeproj (M3/M4, not built yet)

One Xcode project, one bundle ID family, two targets:
- watchOS app: timer landing screen, long-press entry to tutor UI, offline cache.
- iOS companion: full answers, verification queue, content browser.

See CLAUDE.md §7 for the locked app design and watchOS gotchas.
